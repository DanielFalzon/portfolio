//to check how to add enu with values
export interface IToolkitItem {
    id: number,
    name: string,
    logo_url: string,
    type: string,
    level: number,
    years: number
}

export interface INavItem {
    name: string,
    link: string
}